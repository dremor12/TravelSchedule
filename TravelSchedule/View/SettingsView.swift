import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Настройки")
        }
        .background(Colors.viewBackgroundColor)
    }
}

#Preview {
    SettingsView()
}
